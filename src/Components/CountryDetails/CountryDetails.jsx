

const CountryDetails = (country,handleVisitedCountries, handleVisitedFlags) => {
    return (
        <div>
            <p>Country Details:{} </p>
        </div>
    );
};

export default CountryDetails;